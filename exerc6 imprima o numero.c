#include <stdio.h>

int main() {
    int i;

    for (i = 1; i <= 100; i++) { //for para repetir o numero  aquaantidade de vezes pedida
        if (i <= 100) { //if para saber o que fazer dentro do for
            printf("%d\n", i);
        }
    }

    return 0;
}